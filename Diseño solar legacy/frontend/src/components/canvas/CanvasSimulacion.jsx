// This component shows a simulation progress overlay when a simulation is running.
// Since simulation is now handled in Step6Simulacion.jsx synchronously, this is
// a lightweight placeholder that could be used for async progress in the future.
// Currently not mounted (MainCanvas handles visibility).

export default function CanvasSimulacion() {
  // Rendered by MainCanvas only when simulating
  return null
}
