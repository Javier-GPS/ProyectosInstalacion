# Reduced Luminance Tables

The special C2 files supplied for this project are not silently embedded in
the package because their provenance and the difference between the variants
must remain traceable. Copy the selected version here and record its checksum
in the project configuration before production use.
